# ============================================================
# Cesium ion 访问令牌（可选，不填也能运行）
#   1) 免费注册: https://ion.cesium.com/signup/ （只需邮箱，无需信用卡）
#   2) 获取令牌: https://ion.cesium.com/tokens （复制 Default Token，或点 Create token 新建）
#   3) 真实令牌是一长串 JWT，以 eyJ 开头，形如：
#      eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxx.xxxxxxxx
#   4) 建议在 ion 后台限制该令牌：Asset scopes 只勾只读资源、Allowed URLs 只填你的域名
#   ⚠️ 不要使用 ion 后台的 Secret token（那个只能放服务端）
#   不填 → 使用 ESRI 免费卫星底图 + 平面地形；填写 → ion 官方影像 + 高精度 3D 地形
# ============================================================
# VITE_CESIUM_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxx.xxxxxxxx

# ============================================================
# 战役数据 API 地址（可选）
# 默认（未配置时）使用腾讯云函数 URL：
#   https://1490480930-1axi29hdyw.ap-beijing.tencentscf.com
# 需要切换到别的后端时在此覆盖（必须是完整的 https:// 地址）：
#   VITE_API_BASE=https://api.your-domain.com
# 注意：填写畸形的值（Markdown 链接、缺 https:// 等）会被自动忽略并回退到默认地址
# 修改后需重新构建前端（npm run build / 重新部署前端）才生效
# ============================================================
# VITE_API_BASE=https://api.your-domain.com
